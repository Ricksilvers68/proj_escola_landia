<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      position: relative;
      min-height: 100vh;
      margin: 0;
      background-image: url('/images/img3.jpeg');
      background-size: 80%; /* Reduz visualmente a imagem */
      background-repeat: no-repeat;
      background-position: center;
      background-attachment: fixed;
    }

    /* ✅ Camada translúcida por cima da imagem de fundo */
    .background-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.3); /* branco com 40% de opacidade */
      z-index: 0;
    }

    .login-container {
      max-width: 500px;
      margin: 60px auto;
      padding: 30px;
      background-color: rgba(255, 255, 255, 0.85);
      border-radius: 10px;
      box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
      position: relative;
      z-index: 1;
    }

    .form-label {
      font-weight: 500;
    }

    .alert {
      margin-top: 10px;
    }

    footer {
      position: relative;
      z-index: 1;
    }
  </style>
</head>
<body>
  <!-- ✅ Camada de transparência sobre a imagem -->
  <div class="background-overlay"></div>

  <div class="login-container">
    <h2 class="text-center mb-4">
      <span style="color: darkkhaki; font-size: 20px;">Olá!<br>Bem vindo ao</span><br>Controle de Pontualidade Escolar
    </h2>

    <!-- Formulário -->
    <form action="/login" method="POST" autocomplete="off">
      <div class="mb-3">
        <label for="usuario" class="form-label">Usuário</label>
        <input type="text" class="form-control" id="usuario" name="usuario" autocomplete="off" required />
      </div>
      <div class="mb-3">
        <label for="senha" class="form-label">Senha</label>
        <input type="password" class="form-control" id="senha" name="senha" autocomplete="new-password" required />
      </div>
      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Entrar</button>
      </div>
    </form>
  </div>

  <footer class="sm-light text-muted py-3 fixed-bottom">
    <div class="container d-flex justify-content-around align-items-center flex-wrap">
      <small>&copy; 2025 EE PROFESSORA LANDIA SANTOS BATISTA</small>
      <small>Endereço: Rua Professor Engenheiro Claudio Abraão, 210 - Ferraz de Vasconcelos/SP</small>
    </div>
  </footer>
</body>
</html>