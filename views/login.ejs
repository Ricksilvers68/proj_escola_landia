<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Login - Controle de Pontualidade</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
  <style>
    body {
      background: linear-gradient(#dee4ec, #177ce99a);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-box {
      max-width: 400px;
      background: #ffffffa6;
      padding: 2.5rem;
      border-radius: 12px;
      box-shadow: 0 0 18px rgba(0,0,0,0.15);
    }
    .login-title {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
      color: #34495e;
      font-weight: bold;
      text-align: center;
    }
    .btn-login {
      width: 100%;
    }
    .form-label {
      color: #555;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <div class="login-title">Controle de Pontualidade Escolar - CPE</div>
    <% if (erro) { %>
      <div class="alert alert-danger py-2"><%= erro %></div>
    <% } %>
    <form method="POST" action="/login">
      <div class="mb-3">
        <label for="usuario" class="form-label">Usuário</label>
        <input type="text" class="form-control" id="usuario" name="usuario" required>
      </div>
      <div class="mb-3">
        <label for="senha" class="form-label">Senha</label>
        <input type="password" class="form-control" id="senha" name="senha" required>
      </div>
      <button type="submit" class="btn btn-primary btn-login mt-3">Entrar</button>
    </form>
  </div>

  <!-- FOOTER -->
<footer class="bg-light text-muted py-3 fixed-bottom">
  <div class="container d-flex justify-content-around align-items-center flex-wrap">
    <small>&copy; 2025 EE PROFESSORA LANDIA SANTOS BATISTA</small>
    <small>Endereço: Rua Professor Engenheiro Claudio Abraão, 210 - Ferraz de Vasconcelos/SP</small>
    <small>Conheça a nossa <a href="#">Política de Privacidade</a></small>
  </div>
</footer>

</body>
</html>
</body>
</html>