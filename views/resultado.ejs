<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Resultado da Busca</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    @media print {
      .no-print {
        display: none;
      }
    }
/* Cabeçalho para exibição e impressão */
.cabecalho {
    text-align: center;
    margin: 20px 0;
}
.cabecalho img {
    max-width: 80px;
}
.cabecalho h1 {
    font-size: 1.5rem;
    margin: 5px 0;
}
.cabecalho h2 {
    font-size: 1rem;
    font-weight: normal;
    color: #555;
    margin: 0;
}

/* Ocultar botões e links ao imprimir */
@media print {
    .no-print {
        display: none;
    }
}
</style>
</head>
<body class="bg-light">

<!-- CABEÇALHO PARA IMPRESSÃO -->
<div class="cabecalho">
    <h1>EE Prof. Landia Santos Batista</h1>
    <h2>CONTROLE DE PONTUALIDADE ESCOLAR</h2>
</div>
  

</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="mb-4">Resultado da Busca</h1>

        <% if (typeof erro !== 'undefined' && erro) { %>
          <div class="alert alert-danger"><%= erro %></div>
        <% } %>

        <% if (typeof aluno !== 'undefined' && aluno) { %>
          <p><strong>Nome:</strong> <%= aluno.nome %></p>
          <p><strong>RA:</strong> <%= aluno.ra %></p>
          <p><strong>Horário de Entrada:</strong> <%= horaAtual %></p>
        <% } else if (!erro) { %>
          <p class="text-muted">Nenhuma informação para exibir.</p>
        <% } %>

        <div class="mt-4 no-print">
          <a href="/buscar" class="btn btn-secondary">Nova Busca</a>
          <% if (aluno) { %>
            
            <button onclick="window.print()" class="btn btn-success">Imprimir</button>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>