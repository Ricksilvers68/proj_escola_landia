<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Confirmação de Entrada</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }

    .container {
      margin-top: 60px;
    }

    .cabecalho {
      text-align: center;
      margin-bottom: 30px;
    }

    .cabecalho h1 {
      font-size: 1.5rem;
      margin: 0;
    }

    .cabecalho h2 {
      font-size: 1rem;
      color: #555;
    }

    .card {
      border: none;
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <div class="cabecalho">
      <h1>EE Prof. Landia Santos Batista</h1>
      <h2>Controle de Pontualidade Escolar</h2>
    </div>

    <div class="card shadow-sm mx-auto" style="max-width: 500px;">
      <div class="card-body">
        <% if (erro) { %>
          <% if (erro.includes('já registrou a entrada hoje')) { %>
            <div class="alert alert-warning fw-bold" role="alert">
              <%= erro %>
            </div>
          <% } else { %>
            <div class="alert alert-danger fw-bold" role="alert">
              <%= erro %>
            </div>
          <% } %>

          <a href="/buscar" class="btn btn-secondary mt-3">Tentar Novamente</a>

        <% } else if (aluno) { %>
          <h4 class="mb-3 text-success">✅ Registro efetuado com sucesso!</h4>
          <p><strong>Nome:</strong> <%= aluno.nome %></p>
          <p><strong>RA:</strong> <%= aluno.ra %></p>
          <p><strong>Horário:</strong> <%= horaAtual %></p>
          <p><strong>Justificativa:</strong> <%= justificativa || 'Nenhuma' %></p>  
          <a href="/buscar" class="btn btn-primary mt-4">Voltar</a>

        <% } else { %>
          <div class="alert alert-warning fw-bold">⚠️ Nenhuma informação para exibir.</div>
          <a href="/buscar" class="btn btn-secondary mt-3">Voltar</a>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>