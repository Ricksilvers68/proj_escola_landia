<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Resultado da Busca</title>
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-body">
            <h1 class="mb-4">Resultado da Busca</h1>

            <% if (erro) { %>
                <div class="alert alert-danger"><%= erro %></div>
            <% } %>

            <% if (aluno) { %>
                <ul class="list-group">
                    <li class="list-group-item"><strong>Nome:</strong> <%= aluno.nome %></li>
                    <li class="list-group-item"><strong>RA:</strong> <%= aluno.ra %></li>
                    <li class="list-group-item"><strong>Hora da Busca:</strong> <%= horaAtual %></li>
                </ul>
            <% } else if (!erro) { %>
                <div class="alert alert-warning">Nenhum aluno encontrado com esse nome ou RA.</div>
            <% } %>

            <div class="mt-3">
                <a href="/buscar" class="btn btn-secondary">Voltar</a>
            </div>
        </div>
    </div>
</div>

</body>
</html>