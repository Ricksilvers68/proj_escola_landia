<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Buscar Aluno</title>
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
    <style>
        
        /* Animação simples para o cartão */
        .card {
            opacity: 0;
            transform: translateY(30px);
            animation: slideUp 0.5s ease-out forwards;
        }
        @keyframes slideUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body class="bg-info">

<!-- CABEÇALHO -->
<header class="text-white text-center py-3">
  <h1 class="h4">E E Professora Landia Santos Batista</h1>
  <h2 class="h1">CONTROLE DE PONTUALIDADE ESCOLAR - CPE</h1>
</header>

<!-- FORMULÁRIO CENTRALIZADO NA METADE DA TELA -->
<div class="d-flex justify-content-center" style="margin-top:5vh;">
    <div class="card shadow-sm p-3" style="width:100%; max-width: 450px;">
        <div class="card-body">
            <h1 class="mb-5 text-center">Caro aluno,<br> por favor, identifique-se</h1>
            <form method="POST" action="/buscar">
    <div class="mb-4">
        <label for="nome" class="form-label fw-bold text-dark">Nome do Aluno:</label>
        <input 
            type="text" 
            id="nome" 
            name="nome" 
            class="form-control" 
            placeholder="Ex: João da Silva"
            oninput="this.value = this.value.replace(/[0-9]/g, '')"
            autocomplete="off"
            required
        >
    </div>
    <div class="mb-4">
        <label for="ra" class="form-label fw-bold text-dark">RA:</label>
        <input 
            type="text" 
            id="ra" 
            name="ra" 
            class="form-control" 
            placeholder="Ex: 123456"
            oninput="this.value = this.value.replace(/\D/g, '')"
            autocomplete="off"
            required>
    </div>

    <div class="mb-3">
  <label for="justificativa" class="form-label">Justificativa (opcional)</label>
  <textarea id="justificativa" name="justificativa" class="form-control" rows="3" placeholder="Descreva o motivo do atraso, se quiser..."></textarea>
</div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-info btn-lg" type="submit">Registrar</button>
    </div>
</form>
        </div>
    </div>
</div>

<!-- FOOTER -->
<footer class="bg-light text-muted py-3 fixed-bottom">
  <div class="container d-flex justify-content-around align-items-center flex-wrap">
    <small>&copy; 2025 EE PROFESSORA LANDIA SANTOS BATISTA</small>
    <small>Endereço: Rua Professor Engenheiro Claudio Abraão, 210 - Ferraz de Vasconcelos/SP</small>
    <small>Conheça a nossa <a href="#">Política de Privacidade</a></small>
  </div>
</footer>

</body>
</html>