<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Buscar Aluno</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
  position: relative;
  min-height: 100vh;
  margin: 0;
  padding-bottom: 80px;
  background-image: url('/images/img5.jpeg');
  background-size: 75% auto;
  background-position: center;
  background-repeat: no-repeat;
  background-attachment: fixed;
}

/* Camada escura leve */
.background-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  
  z-index: 0;
}

/* Container reduzido */
.login-container {
  width: 100%;
  max-width: 400px;
  margin: 10px auto;
  padding: 20px;
  background-color: rgba(255, 255, 255, 0.85);
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
  position: relative;
  z-index: 1;
  max-height: 100vh;
  overflow-y: auto;
}

/* Ajustes finos de layout */
.card {
  background: transparent;
  border: none;
  animation: slideUp 0.5s ease-out forwards;
}

@keyframes slideUp {
  to {
    transform: translateY(0);
  }
}

footer {
  background-color: #f8f9fa;
  color: #6c757d;
  text-align: center;
}

footer a {
  color: #007bff;
  text-decoration: underline;
}
</style>

</head>
<body>

  <!-- Camada de fundo translúcida -->
  <div class="background-overlay"></div>


  <!-- Formulário centralizado -->
<div class="d-flex justify-content-center">
  <div class="card shadow-sm p-3" style="width:100%; max-width: 500px;">
    <div class="login-container">
      <h2 class="text-center mb-3">Caro aluno,<br>por favor, identifique-se</h2>
      <form method="POST" action="/buscar">
        <div class="mb-3">
          <label for="nome" class="form-label fw-bold text-dark">Nome:</label>
          <input type="text" name="nome" id="nome" class="form-control" required autocomplete="off" />
        </div>
        <div class="mb-3">
          <label for="ra" class="form-label fw-bold text-dark">RA:</label>
          <input type="text" name="ra" id="ra" class="form-control" required autocomplete="off" />
        </div>
        <div class="mb-3">
          <label for="justificativa" class="form-label">Justificativa (opcional)</label>
          <textarea name="justificativa" id="justificativa" class="form-control" rows="2"></textarea>
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-info">Registrar</button>
        </div>
      </form>
    </div>
  </div>
</div>

  <!-- Rodapé -->
  <footer class="text-muted py-3 fixed-bottom">
    <div class="container d-flex justify-content-around align-items-center flex-wrap">
      <small>&copy; 2025 EE PROFESSORA LANDIA SANTOS BATISTA</small>
      <small>Endereço: Rua Professor Engenheiro Claudio Abraão, 210 - Ferraz de Vasconcelos/SP</small>
    </div>
  </footer>

</body>
</html>