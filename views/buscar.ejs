<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Buscar Aluno</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
  body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    background-repeat: no-repeat;
    background-position: center;
    background-size: 100% auto;
    display: flex;
    flex-direction: column;
  }

  .main-wrapper {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .login-container {
    width: 100%;
    max-width: 450px;
    padding: 25px;
    background-color: rgba(255, 255, 255, 0.88);
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
  }

  footer {
    background-color: #f8f9fa;
    text-align: center;
    padding: 10px;
    font-size: 0.9rem;
    color: #6c757d;
  }

  /* ✅ Responsividade apenas do formulário */
  @media (max-width: 768px) {
    .login-container {
      width: 90%;
      padding: 10px;
      margin-top: 50px;
      font-size: 0.95rem;
    }

    @media (max-width: 768px) {
  .login-container .form-control {
    margin-bottom: 2px; /* espaçamento entre inputs */
  }

  .login-container textarea {
    margin-bottom: 25px;
  }
}

    h2 {
      font-size: 1rem;
    }
  }
  </style>
</head>

<body data-desktop="/images/civico2.jpeg" data-mobile="/images/militar3.png">
  <div class="main-wrapper">
    <div class="login-container">
      <h1 class="text-center mb-5">Caro aluno,por favor,<br> identifique-se</h1>
      <br>
      <br>
      <br>
      <br>
      


      <form method="POST" action="/buscar">
        <div class="mb-3">
          <label for="nome" class="form-label fw-bold text-dark">Nome:</label>
          <input type="text" name="nome" id="nome" class="form-control" required autocomplete="off" />
        </div>
        <div class="mb-3">
          <label for="ra" class="form-label fw-bold text-dark">RA:</label>
          <input type="text" name="ra" id="ra" class="form-control" required autocomplete="off" />
        </div>
        <div class="mb-3">
          <label for="justificativa" class="form-label">Justificativa (opcional)</label>
          <textarea name="justificativa" id="justificativa" class="form-control" rows="2"></textarea>
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-info">Registrar</button>
        </div>
      </form>
    </div>
  </div>

  <footer>
    &copy; 2025 EE PROFESSORA LANDIA SANTOS BATISTA
  </footer>

  <script>
    const body = document.body;
    const isMobile = window.innerWidth <= 768;
    const newBg = isMobile ? body.dataset.mobile : body.dataset.desktop;
    body.style.backgroundImage = `url('${newBg}')`;
  </script>
</body>
</html>